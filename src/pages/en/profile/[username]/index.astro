---
import Page, {
  getStaticPaths as originalGetStaticPaths,
} from '@/pages/profile/[username]/index.astro';

export const getStaticPaths = originalGetStaticPaths;
const { props } = Astro;
---

<Page {...props} />
