---
import { languageTag } from '@/i18n/paraglide/runtime';

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>{date.toLocaleDateString(languageTag(), {
      dateStyle: 'medium',
})}</time>
